{
  "description": "Auto-format markdown on Write/Edit; final lint check on Stop",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); echo \"$input\" | jq -re '.tool_input.file_path // \"\" | select(endswith(\".md\"))' > /dev/null 2>&1 && echo \"$input\" | bash ${CLAUDE_PLUGIN_ROOT}/scripts/mdlint.sh || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/mdlint-check.sh"
          }
        ]
      }
    ]
  }
}
