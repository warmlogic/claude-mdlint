{
  "description": "Auto-format markdown on Write/Edit; final lint check on Stop",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if printf '%s' \"$input\" | jq -e '(.tool_input.file_path // \"\") | endswith(\".md\")' >/dev/null 2>&1; then printf '%s' \"$input\" | bash ${CLAUDE_PLUGIN_ROOT}/scripts/mdlint.sh; fi"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/mdlint-check.sh"
          }
        ]
      }
    ]
  }
}
